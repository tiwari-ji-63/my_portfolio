# Email Configuration for Portfolio Admin

# Development Settings (Optional)
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Email Settings - Choose ONE of the following options:

# Option 1: Gmail SMTP (for development/testing)
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_FROM=your-gmail@gmail.com

# Option 2: Custom SMTP Server (for production)
# SMTP_HOST=smtp.your-domain.com
# SMTP_PORT=587
# SMTP_USER=noreply@your-domain.com
# SMTP_PASS=your-smtp-password
# EMAIL_FROM=noreply@your-domain.com

# Option 3: SendGrid (recommended for production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# EMAIL_FROM=noreply@your-domain.com

# Database (already configured)
# MONGO_URL=mongodb://localhost:27017/portfolio

# Notes:
# - If no email configuration is provided, tokens will be displayed on screen
# - For Gmail, you need to generate an App Password (not your regular password)
# - In development, Ethereal Email is used automatically for testing
# - Copy this file to .env and uncomment the configuration you want to use
